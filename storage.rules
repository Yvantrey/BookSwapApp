rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to upload and read book images
    match /book_images/{imageId} {
      allow read: if true; // Anyone can read book images
      allow write: if request.auth != null; // Only authenticated users can upload
    }
  }
}